pipeline {
    agent {
        label 'deployment-server'
    }

    stages {
        stage('Example') {
            steps {
                echo 'Hello, Jenkins!'  // Print a message to the console
            }
        }
    }
}